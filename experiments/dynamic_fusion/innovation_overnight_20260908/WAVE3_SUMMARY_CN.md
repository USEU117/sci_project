# 第三轮汇总（2026-09-08 03:04 北京）

## 最值得跟踪的结果
**完整高分辨率输入保留上下文**：两类k2、24个合成mask，DINO full896总体AP .561246，相对full448 .507251提高 .053995，相对tile64提高 .083205。两类scratch相对full448分别+ .120583 / + .129019；cutpaste仍分别− .021168 / − .012456，但远小于裁块损失。这是当前质量方向的主要候选信号，尚未扩类确认，更不是论文新算法证实。full896与tile相同token总数不等于等算力；全图attention work proxy约4倍。

**存储压缩已获不同shot/网格确认**：k4/native32，6类24 folds，3072行memory，FP16存储0.5倍、NN一致99.9859%、宏ΔAP约−0.00000043；INT8含scale存储0.250326倍、NN一致99.5581%、宏ΔAP约−0.000129。保存全部memory行，decode后仍用float32查询；仅支持存储/传输工程改进，不声称推理加速或精度提高。三个合成族都使用原mask，实际全3变体，unique episode数与方法行数需分开。

## 负结果与边界
- 低层高频/梯度：2类24mask，raw高频等权AP .004417 vs同脚本DINO .421633，接近面积基线，无独立信号；旧Haar/Sobel亦有负结果，此轮只算工程诊断，不算新颖算法。
- 光度memory：独立审计发现尺度不变的nuisance-clean AUC从 .537370降至 .522443（12/12 folds方向相同），有弱支持；但不等于缺陷检测改善。绝对score偏移减少受memory扩张收缩影响。留族臂使用query family角色，只作诊断；all5部署臂不读取该角色。
- full448绝对AP在不同脚本不同，必须追踪后处理/插值口径，不跨脚本比较。审计任务已派发。

## 第四轮安排
1. full896补其余4类，以相同脚本full448控制比较，保留4类新结果及6类合并，避免只看先前2类。
2. 两类缓存上的固定等权双尺度feature concat，对比单尺度与late score均值；检验上下文+细节联合匹配，参数不扫描。
3. 跨脚本指标/unique样本计数审计。

若高分辨率扩类仍有可用信号，剩余时间可做冻结配置真实MPDD诊断（明确不是新独立确认集、不拟合选参）。若不稳健即归档，不靠少量阳性类别改规则。

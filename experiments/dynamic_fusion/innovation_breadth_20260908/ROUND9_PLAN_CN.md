# 广度探针 R9 预注册（2026-09-09 夜，续）— 距离度规 / 位置先验轴

背景：R7/R8 已关闭匹配尺度/通道/密度与"尺寸假设"；累计 28 族负。R9 补两个仍未被单独测过的廉价轴
（全部离线，冻结 A1 concat 特征）：

| 族 | 轴 | 成员 | 机制 | 假设 |
|---|---|---|---|---|
| DST 距离度规 | 度量空间 | DST_L1、DST_cheb | 行 unit 后的 concat 特征：top-1 用 L1（曼哈顿）与 Chebyshev(L∞) 代替冻结的 1−cos(L2)。行保持 unit（仅比较几何改变） | 余弦强调主导方向差；L1/L∞ 对"全局小差异"更敏感。观察度规是否携带 A1 之外信息（R7 NDW 提示近中性） |
| PLC 位置锁定匹配 | 位置先验（对照） | PLC（cell(i,j) 只在 refs 的同类位置 (i,j) 上找 top-1） | 显式测试"无配准位置先验"假设（MPDD 括号件存在旋转/位姿差异） | 预期深负（GV/COP/跨旋转失败同源）——作为**对照性证伪**：确认位置锁定类先验在无配准时不可用，一次补齐该证据链 |

预注册门同全 breadth（lead 宏 ΔAP ≥ +0.01 且 worst ≥ −0.03 且 ΔAUROC ≥ −0.005，k2/k4 均须）；
parity k2 0.343706 / k4 0.388328；零 test 标签拟合；不做门后调参。
预期：DST 近中性~弱负（≤ +0.005 量级），PLC 深负；若任何成员过门才升格。

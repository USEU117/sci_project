# Failed sidecar verification

十二类均在异常样本的掩码复核处失败。每类约有 100 个异常样本不匹配，正常样本没有出现该问题。

原因是复核脚本直接缩放了值为 0/1 的 PNG，而 AnomalyCLIP `dataset.py` 会先执行 `mask > 0`，再乘 255，然后才进行 PIL/torchvision 缩放。两条预处理路径不一致。

处理：

1. 保留本次失败报告；
2. 修正复核脚本，使其逐步复制旧加载器的掩码流程；
3. 使用新的输出目录重跑，禁止覆盖本次失败记录。

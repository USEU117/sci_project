# WAVE2 ARCHIVE — S0-DG-SAFE 可靠性保护融合被冻结公式实证拒绝（2026-09-03 03:40）

## 判决
- **wave2_passed = False**（任务书 16 §2.7 Wave 2 门）：
  - cond1 Spearman ρ(r_sub, ΔSUB−A1 Pixel-AP) = **+0.3387 < 0.40**（18 配置，seed0，448 协议）
  - cond2 connector r_sub 未落入后 25%（connector k1/k2/k4 = 0.315/0.333/0.111；q25 = 0.301）
- 按 16 §2.7：失败 → **禁止训练标签 router**；按 §6：失败即停、S0 不自动转 Wave3。
- 结论：**S0-DG-SAFE（双几何可靠性保护融合）归档**。A1 维持主方法；Wave1 已单独证明
  “固定均值组合 +0.0164”存在，但**正常-only 稳定性无法可靠识别何时信任 SUB**（尤其
  connector），因此带保护的融合 route 不成立（在 doc 自冻结公式下）。

## 数据与机制诊断（reliability_raw.json，18 行全量）
| 现象 | 数值 | 机制 |
|---|---|---|
| B_tail 逐类完全相同、仅随 k 变 | k1=1.7047, k2=2.3026, k4=2.9444 = log((2+9k)/2) | 逐像素池内最大 z 恒为
  −log(2/(2+n))，且占池 ~1/n>1% → P99 恒打到该量化上限 → **B_tail 为 k 的纯函数，无类别信号** |
| U_layer(k1) 多类恒等 / 部分类=0 | bracket_black/brown/white/connector=0.1054；metal_plate/tubes=0.0000 | 单参考 k1 池仅 9 值，z 量化；
  当身份图被 B/C 子空间近乎完全重建时 z 图全同 → 中位差 0/固定量化值 |
| U_aug 是唯一有类别差异的分量 | 0.39–0.54 | 增强后残差 z 变化；但跨类别差异小 |
| r_sub 排名主要由 U_aug 与小池量化噪声决定 | ρ=0.339 | 无法识别 connector（其 SUB 最差但正常
  稳定性并不更差） |

## 对论文的意义（诚实口径）
1. Wave1 的正结果（固定 mean 组合 pooled Δ=+0.0164）是**不可部署选择器**的静态事实；
   Wave2 证明 doc 提出的“正常-only 稳定性保护门”在该小池校准形式下不可行。
2. B_tail 型“逐像素经验尾概率 + 跨池 P99”在 k≤4 few-shot 下存在**量化退化**——这是对
   任务书 §2.3-2.4 校准设计本身的实证缺陷记录，可作为未来改进方向（改用分布级/平滑尾估计）。
3. A1 仍是主方法；本文档与 WAVE1/WAVE2 JSON 一起构成“负结果 + 机制解释”证据链。

## 后续（用户已授权夜间自主探索）
- P5-B（S2-GPMR 责任熵预检）已单独归档（|ρ|<0.3）。
- 剩余 GPU 时间转向 P5-A（S1-HGLC 图像级全局—局部一致性诊断）与 P5-C（同主干不同分支
  聚合互补性，纯 CPU），用于回答“两个视觉分支结合是否创新不足”的路线问题。

# 结论

WP3、WP4 和 WP5 的 CPU 工程接口已经可以使用：

- 视觉单分支与文本单分支采用统一输出格式；
- 固定视觉权重候选为 0、0.25、0.5、0.75、1；
- 已实现概率熵、分支一致/冲突、像素响应集中程度、正常参考增强一致性和跨 shot 敏感度；
- 固定权重只能预先声明，禁止根据测试指标选择。

本次完全使用合成概率，只证明公式、形状和数值范围正确，不证明方法性能。正式运行必须等待 WP2 真实校准参数完成。

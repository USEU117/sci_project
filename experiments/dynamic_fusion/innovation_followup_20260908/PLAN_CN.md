# 2026-09-08 后续创新探索

用户新授权：继续多方法探索。此轮为当前交互内的有界工作，不重启昨夜自动化，不适用昨夜07:00截止。旧论文与冻结A1保持不变。

## 第一批独立方向

1. **误差分解**：现有458张真实图，分离异常图内AP、跨图pooled AP、正常高尾和面积分层；只读诊断，不使用test拟合参数。
2. **跨输入尺度的剩余信息**：将高分辨率特征相对低分辨率的残差独立拿出来检验，而非整段concat。直接残差或normal-only预测残差，保留high/low/late均值与错配控制。
3. **参考图身份一致性**：按参考图分别NN，再固定稳健聚合，区别于删除patch或挑图；所有memory图保留。先审计旧路线是否重复。
4. **精确检索剪枝**：以Euclidean三角下界保留精确NN，测候选比例及完整时间。高维集中可能使其失败，照实记录。

每条30分钟左右首轮、CPU2线程、优先复用缓存；所有候选先固定协议。合成探针必须使用原像素mask，thin_scratch不因缩小mask而漏评。不同方法绝对AP只在同脚本配对比较。真实test数据已经开发使用，不称新独立确认。

## 新颖性门

残差、参考集合聚合、三角界精确搜索本身均不是原创概念。本轮只检验本项目是否存在可用新信息与资源优势。有信号后还须对照论文、等预算和简单机制，再决定是否深研。

## 输出

脚本：`scripts/innovation_followup_20260908/`；结果在本目录各方向子目录。最终形成SUMMARY_CN.md，列实测、负结果、未完成项与下一步。若子代理额度不可用，root接手有界任务，不反复请求失败调用。
